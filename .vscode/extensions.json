{
  "recommendations": [
    "llvm-vs-code-extensions.vscode-clangd",
    "ms-vscode.cmake-tools",
    "vadimcn.vscode-lldb",
    "editorconfig.editorconfig"
  ],
  // ms-vscode.cpptools is OPTIONAL: only needed for the cppvsdbg (MSVC) and cppdbg/gdb (MinGW)
  // debug configs in launch.json. Its IntelliSense/formatting/analysis are disabled in
  // settings.json, so it won't compete with clangd. Clang-only users don't need it.
  "unwantedRecommendations": []
}
